# Fizz Buzz

## Build

Build the app with :

```bash
mvn clean install
```

## Execute

After the build, execute the app by executing :
```bash
mvn exec:java -Dexec.mainClass="com.issam.fizzbuzz.App"
```
